# ADR-005: MCP Posture

The MCP server uses rmcp over stdio and exposes only index/search/status tools. It does not execute user-provided commands. Logs and diagnostics are written to stderr so stdout stays reserved for protocol messages.
